<html lang="en">
<head>
  <%= stylesheet_link_tag 'https://cdn.datatables.net/1.13.4/css/jquery.dataTables.min.css' %>
</head>
<body>
  <div class="container mt-5">
    <div class="<%=flash.empty? ? 'px-4 py-5 my-2' : 'px-4 pb-5 mb-5' %> text-center">
      <%= render "shared/flash_messages" %>
    </div>
    <h1 class="display-5 fw-bold" align="center" style="color: #003262">Manage Courses</h1>
    <br>
    <div class="mb-3">
      <%= link_to "Export", export_courses_path(format: :csv), class: "btn btn-secondary" %>
      <%= link_to "Add New Course", new_course_path, class: "btn btn-success mb-3 ms-2" %>
    </div>
    <table id="courses-table" class="display" style="width:100%">
      <thead>
        <tr>
          <th>Code</th>
          <th>Title</th>
          <th>Units</th>
          <th>Semester</th>
          <th>Schedule</th>
          <th>Available</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <% @courses.each do |course| %>
          <tr>
            <td><%= course.code %></td>
            <td><%= course.title %></td>
            <td><%= course.units %></td>
            <td><%= course.semester %></td>
            <td><%= course.schedule %></td>
            <td><%= course.available ? "Yes" : "No" %></td>
            <td>
              <%= link_to "Edit", edit_course_path(course), class: "btn btn-primary btn-sm" %>
              <%= button_to "Delete", destroy_course_path(course), 
                  method: :delete, 
                  data: { confirm: "Are you sure you want to delete this course?" }, 
                  class: "btn btn-danger btn-sm", style: "margin-top: 5px;" %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
    
    <%= link_to "back to admin dashboard".html_safe, admins_path, class: "btn btn-secondary mt-5 btn-lg px-4 gap-3" %>
  </div>

  <%= javascript_include_tag 'https://code.jquery.com/jquery-3.6.0.min.js' %>
  <%= javascript_include_tag 'https://cdn.datatables.net/1.13.4/js/jquery.dataTables.min.js' %>
  <script>
    $(document).ready(function() {
      $('#courses-table').DataTable();
    });
  </script>
</body>
</html>